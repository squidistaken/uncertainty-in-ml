[project]
name = "uncertainty-in-ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kaggle>=2.2.0",
    "pre-commit>=4.6.0",
    "ruff>=0.15.14",
]

[tool.ruff]
line-length = 80
